# error_constant_in_archive
this repo demos an error using same global/namespace constant variable in two different sources(.so or executable) would result in double destruction of the same variable.

# steps:
* make
